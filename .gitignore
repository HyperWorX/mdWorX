# Build outputs
build-out/
*.obj
*.lib
*.exp
*.pdb
*.ilk
*.idb
*.dll
*.exe

# Visual Studio
.vs/
*.vcxproj.user
*.suo
*.sdf
*.opensdf
*.aps
*.user
Debug/
Release/
x64/
x86/

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CMakeUserPresets.json

# Node / web layer
node_modules/
web/dist/
web/.cache/
package-lock.json.bak

# Vendored SDK (download per docs/dev-setup.md; not redistributable)
sdk-vendored/

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/launch.json

# Temp / logs
*.log
*.tmp

# Local-only / internal files — not for the GitHub code tree.
# The source tree and the plugin build belong on the main code page; the
# release bundle is attached to GitHub Releases, not committed here.
CHANGELOG.md
docs/*.py
docs/reviews/
release-bundle/

# Screenshot/GIF harness scratch output (regenerated by web/*.mjs).
# The promoted images live in img/ and are tracked; this staging dir is not.
img/regen/

# Local-only hero-GIF intro source (the rendered img/hero.gif is what ships).
img/firstframe.png

# Editor/agent local settings.
.claude/

# Unused line-ending scratch fixtures (the harness reads tests/eol-mixed.md only).
tests/eol-lf.md
tests/eol-crlf.md
tests/eol-crlf-notrailing.md
